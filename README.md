# pwpw

Ideas for my P2P password management + identity app

## Features
 - Should run locally with no dependency on central servers,
 - Should provide option for syncing/backup with user's own data servers,
 - All user data should be fully encrypted,
 - Clients should automatically sync with all user's devices.
